# 결정 기록 — PRD ↔ 디자인 충돌 12건

**진행 상태** **12 / 12 확정 — 완료**
**최종 수정** 2026-09-04
**출처** 작업용 TODO 문서 0장. 이 저장소에 보관하지 않는다

결정 내용은 [requirements.md](./requirements.md) v1.0에 흡수했고, 이 파일에는 결정 이력만 남긴다.

---

## C-1. 감정 세트 — 확정

**표준 FER 7종을 그대로 쓰고, 표시 이름만 디자인 톤으로 한다.**

| 엔진 라벨 | 표시 이름 | 색 |
|---|---|---|
| `happy` | 기쁨 | `#FFD72F` |
| `sad` | 슬픔 | `#5AC8FF` |
| `angry` | 분노 | `#FF5A47` |
| `surprise` | 놀람 | `#8F66FF` |
| `neutral` | 시크 | `#80EFD6` |
| `disgust` | 우웩 | `#B8F16A` |
| `fear` | 무서움 | `#FF3D86` |

- 디자인의 **설렘·당황 폐기** (표준 FER 라벨에 없어 채점 불가)
- 7종이므로 **RD-01의 7라운드 옵션이 성립**하고 RD-02(중복 금지)도 유지
- **13장 미결 1번**(혐오·두려움 포함 여부) → 포함으로 확정

**할 일**
- PRD EM-01 라벨 목록 교체
- FE 감정 토큰 7종 재정의, 우웩·무서움 힌트 문구 신규 작성 (EM-02)
- BE 라벨 매핑 테이블. 표시명은 서버가 `round:revealed`로 내려줌

---

## C-3. 제출 후 화면 — 확정

**PRD 채택.** 제출 즉시 `RoundResult`로 전환하고, 카드가 실시간으로 끼어들며 순위가 재정렬된다.

- `Play.dc.html`의 `phase: "wait"` 대기 화면과 아바타 체크 그리드 **삭제**
- `RoundResult.dc.html`은 이미 점진적 도착(`arrived` 상태, `⏳` placeholder, "실시간 집계 중" 배지)을 구현하고 있어 그대로 쓴다
- 진짜 충돌은 PRD ↔ 디자인이 아니라 **`Play`의 대기 화면 ↔ `RoundResult`의 실시간 집계** 사이였다

**할 일**
- BE `submission:scored` 개별 발행 (RS-01·02·04)
- FE `Play`의 wait 분기 전체 삭제
- **13장 미결 9번**(먼저 제출한 사람의 대기 시간)은 "실시간 집계로 채운다"로 답했으나 베타 관찰 대상으로 남김

---

## C-2. 라운드 결과 화면 구조 — 확정

**디자인 채택.** 큰 사진 캐러셀 + 하단 가로 순위 레일.

- 레일 썸네일이 `translateX` + `cubic-bezier(.22,1.2,.36,1)` 500ms로 이동하며 RS-03의 재정렬 애니메이션을 담당
- 12명 방에서 세로 카드 목록은 스크롤이 길고 셀카가 작아진다

**고쳐야 할 디자인 코드 버그**
```js
const cur = ranked[cursor]      // cursor가 점수순 배열의 인덱스
```
재정렬되면 **보고 있던 사진이 예고 없이 다른 사람으로 바뀐다.**
→ `cursor`를 인덱스가 아니라 `participantId`로 관리할 것.

**할 일**
- PRD RS-02·03 재작성: "카드 목록 재정렬" → "레일 썸네일 재정렬, 큰 사진은 사용자가 넘겨서 본다"

---

## C-4. 리액션 — 확정

**♥와 ⁇ 두 버튼을 모두 살리되, 둘 다 점수에 반영하지 않는다.**

| | ♥ 좋아요 | ⁇ 물음표 |
|---|---|---|
| 의미 | 공감·재미. "이 표정 최고" | "띠용?" — 주제에 맞는 사진인지 의구심 |
| 향하는 대상 | 사진 | **엔진의 판정** (사람 아님) |
| 점수 | **반영 없음** | **반영 없음** |
| 횟수 | 사진당 1회 | 사진당 1회 |
| 복수 사진 | 가능 | 가능 |
| 상호 배타 | 아니오 — 같은 사진에 둘 다 가능 | 동일 |
| 취소 | 토글로 가능 | 토글로 가능 |
| 자기 사진 | 불가 | 불가 |
| 누를 수 있는 시점 | **제출·채점되는 즉시. 최종 정렬을 기다리지 않는다** | 동일 |

- 미제출자는 결과 화면 자체를 못 보므로(RS-11) 자동으로 리액션 불가
- 얼굴 미검출·판정 불가 카드는 **사진이 있으므로 리액션 대상에 포함**한다 (오히려 ⁇의 자연스러운 대상)
- 최종 화면에 ♥와 ⁇를 **나란히** 집계 표시

### 절 재정의

5.9의 성격이 "순위에 반영되는 보너스"에서 "순수 리액션"으로 바뀌므로, 절 이름을 「좋아요」→「**리액션**」으로 바꾸고 `RX-*`로 새로 정의한다. `LK-*`는 폐기 목록으로. (v0.6에서 `RS-01~06`을 같은 방식으로 처리한 전례)

```
폐기  LK-01  최종 정렬 이후 활성화, 10초 동안만  → 실시간 상시 가능
폐기  LK-02  라운드당 1표                      → 사진당 1회, 복수 사진 가능
폐기  LK-05  1표당 5포인트 보너스               → 점수 반영 없음
수정  RS-07  전원 확정 시 좋아요 활성화          → 활성화 개념 삭제
수정  RS-08  "좋아요 시간 10초"                → "결과 감상 시간" (길이는 C-10)
수정  SC-06  포인트 = 순위 포인트 + 좋아요 보너스 → 포인트 = 순위 포인트
수정  FN-02  좋아요 보너스 내역 분리 표시        → 보너스 항목 삭제
승격  LK-08  "가장 사랑받은 표정" P1 → **P0**
유지  LK-03  자기 자신 불가
유지  LK-06  누가 눌렀는지 비공개
유지  LK-07  라운드 종료 시점에 확정, 이후 변경 무시
```

### 닫힌 미결

- **13장 미결 7번** (좋아요 배점) — 배점이 없어져 소멸
- **13장 미결 8번** (좋아요 담합) — 점수가 없으니 몰아줘도 얻을 게 없다. 소멸

### 부작용 — 리액션이 갈 곳이 없어진다

PRD 5.9의 원래 논리는 "점수가 낮아도 웃긴 표정을 좋아요가 살려준다"였다. 점수에서 빼면 그 간극이 안 메워진다. 그래서 **LK-08을 P0로 승격**한다. `FinalRanking.dc.html`이 이미 `mostLiked`를 계산하고 순위 행에 ♥ 카운트를 표시하고 있어 추가 비용은 거의 없다.

### 노출 시간 편향 (기록만)

실시간 리액션이면 **먼저 도착한 사진이 더 오래 노출되어 ♥를 더 받는다.** 점수 반영이 없어 순위에는 영향이 없지만 "가장 사랑받은 표정"에는 유리하게 작용한다. MVP에서는 수용하고 베타에서 관찰한다.

### 고쳐야 할 디자인 코드

```js
const canLike = final && !curIsMe;        // final 조건 제거 → !curIsMe && cardScored
like(id) { likes[id] = (likes[id]||0)+1 } // 증가 아닌 토글로
```

---

## C-6. 미제출 처리 — 확정

**PRD 채택.** 페널티 차단을 유지하고, 자발적으로 넘기는 버튼도 자동 제출도 두지 않는다.

- 미제출자는 결과를 일절 못 보고 전용 안내 화면(화면 9)으로 간다 (RS-11~14)
- 차단은 서버가 결과 이벤트를 **발행하지 않는** 방식 (RS-12)
- `Play`의 "건너뛰기" 버튼 **삭제**
- `Play`의 "시간이 끝나면 마지막 화면이 자동 제출돼요" 문구 **삭제**

**자동 제출을 버린 이유** — 사용자가 셔터를 안 눌렀으면 캡처된 이미지 자체가 없다. 프리뷰 프레임을 몰래 잡아 올리는 건 CP-03(정상 촬영 세션 토큰)과 어긋나고, 본인이 모르는 사이 얼굴이 올라간다는 프라이버시 문제도 된다.

**할 일**
- 화면 9 신규 디자인
- BE 소켓 room 분리
- **13장 미결 11번**(페널티 강도)은 열어둔 채 베타 관찰

---

## C-7. 카메라 권한 미허용자 — 확정

**PM-07 유지.** 권한이 없으면 대기실 입장 자체를 막고 참여 불가 화면(화면 5)에만 머물게 한다. 방 이름도 인원도 보이지 않는다.

- 디자인 로스터의 `denied` 항목 **삭제**
- PM-10은 원안대로 "대기실 인원 2명 이상"
- **13장 미결 6번**(배제되는 한 명)은 유지. 베타에서 권한 거부 이탈률 보고 재검토

### 파생 — RO-07을 고쳐야 한다

대기실에 들어온 사람이 전원 `granted`이므로 "각자의 **카메라 권한 준비 상태**를 실시간 표시"할 대상이 없다. `Lobby`의 준비 상태 프로그레스 바와 `N명 준비 · N/12명`도 항상 100%가 된다.

```
수정  RO-07  참여자 목록 + 카메라 권한 준비 상태  →  참여자 목록 + 연결 상태
수정  Lobby  준비 상태 프로그레스 바              →  정원 대비 인원 (5/12명)
수정  Lobby  로스터 배지 4종                     →  준비 완료 / 입장 중 2종
```

---

## C-5. 제한시간 — 확정

**20초 기본 + 방장이 15 / 20 / 30초 중 선택.**

- `RD-04`를 **P1 → P0로 승격**하고 값도 15/30/45 → **15/20/30**으로 변경
- `RD-03` 기본값 30초 → **20초**
- 라운드 수 칩과 같은 UI 패턴을 재사용한다
- `PATCH /api/rooms/{slug}/settings`가 MVP 범위로 들어온다

**근거** — C-3에서 "제출 즉시 결과 화면"을 택했으므로 빨리 낸 사람의 대기가 곧 남은 시간이다. 30초면 최대 25초, 20초면 15초. 대신 프리뷰 로딩이 느린 기기를 위해 방장이 30초로 올릴 수 있게 남겨둔다.

**문구 수정**
```
Play.dc.html       seconds 기본값 30 → 20
Landing / Lobby / Countdown   "20초" 그대로 유지
```

---

## C-8. 참여 경로와 방 식별자 — 확정

**링크 전용.** URL에 12자 난수 slug를 쓰고 `CODE` 배지는 삭제한다.

```python
slug = secrets.token_urlsafe(9)   # 12자
```

- `emoselfie.app/r/Xk9mQ2vB7nLp`
- `Lobby`의 `CODE 7XQ2` 배지 **삭제**
- RO-01·05 그대로 유지, 입력란 없음

**4자리를 버린 이유** — 영숫자 4자리는 `36⁴ ≈ 168만`이다. 7.1의 목표인 동시 100방을 대입하면 임의 시도가 살아있는 방에 걸릴 확률이 **1/16,800**이고, 초당 10회면 평균 28분에 한 번 남의 방에 입장한다. 걸린 순간 낯선 사람의 셀카가 뜬다.

---

## C-10. 결과 감상 시간 — 확정

**`min(60, 10 + 인원수×2)`초. 단, 결과를 보는 참여자 전원이 스킵을 누르면 즉시 다음 라운드로.**

| 인원 | 2명 | 5명 | 8명 | 12명 |
|---|---|---|---|---|
| 감상 시간 | 14초 | 20초 | 26초 | 34초 |

C-4로 이 시간의 성격이 바뀌었다. 원래는 "좋아요를 누를 수 있는 시간"이었는데, 리액션이 실시간 상시 가능해졌으므로 이제는 **결과를 보는 시간**이다.

C-2에서 캐러셀을 택했으므로 사진을 한 장씩 넘겨서 본다. 12명이면 12장이고, 10초 고정으로는 장당 0.8초라 얼굴도 못 알아본다. 인원 비례가 맞다.

### 전원 스킵 — RD-07의 대칭 구조

```
RD-07  전원이 제출하면      → 남은 시간 무관하게 라운드 종료
C-10   전원이 스킵을 누르면  → 남은 시간 무관하게 다음 라운드
```

- 스킵 버튼이 **방장 전용이 아니게 된다.** 디자인의 `isHost ? "스킵" : "다음 라운드까지"(비활성)`를 전원 활성으로
- `3 / 5 넘길 준비` 현황 표시 필요. RD-09의 제출 현황과 같은 패턴 재사용
- **미제출자는 분모에서 제외.** 페널티 화면에는 결과가 없어 스킵 버튼도 없으므로, 분모에 넣으면 전원 스킵이 영원히 성립하지 않는다
- 스킵도 리액션처럼 **토글로 취소 가능**
- 부수 효과 — 방장 이탈 60초 유예 중(RO-10)에도 남은 사람들끼리 스킵으로 게임을 굴릴 수 있다

**필요한 이벤트**
```
round:skip        C→S   토글
round:skipStatus  S→C   { skipped, total }
```

**수정**
```
수정  RS-08  "좋아요 시간 10초 · 방장은 즉시 넘길 수 있다"
       → "감상 시간 min(60, 10+인원×2)초 · 방장이 넘기거나
          결과를 보는 참여자 전원이 스킵하면 즉시 넘어간다"
```

---

## C-9. 방 이름 — 확정

**넣지 않는다.** PRD 그대로 방에 이름을 두지 않는다.

- `Room.name` 컬럼 없음
- `Landing`의 방 이름 입력 카드와 랜덤 버튼 **삭제**
- `Lobby` 헤더의 `{{ roomName }}` **삭제** — 대신 `5라운드 · 20초 · 최대 12명` 같은 설정 요약으로 채운다

---

## C-11. 결과 공유 — 확정

**FN-04는 P1 유지. 버튼은 그리되 비활성으로 둔다.**

- `FinalRanking` 하단 2버튼 레이아웃(`결과 공유` / `방 나가기`) 유지
- 공유 버튼은 회색 + 섀도 없음. 토큰 문서 규칙에 따라 **비활성 이유 문구를 반드시 동반**한다
- 활성화 시점은 M4(비공개 베타) 이후

---

## C-12. 최종 화면 CTA — 확정

**"다시하기"를 MVP에서 뺀다.** 한 사이클이 끝나면 방은 거기서 끝이고, 다음 판은 새 방을 만들어 링크를 다시 공유한다.

```
폐기  FN-03  다시하기(방장만, 같은 인원 유지)  →  P2로 이관
수정  FinalRanking  primaryLabel: isHost ? "한 판 더 하기" : "새 방 만들기"
              →  전원 동일하게 "새 방 만들기"
```

방장·참여자 구분이 사라져 최종 화면이 단순해진다. 누구든 새 방을 만들고 방장이 될 수 있다.

### 파생 — 방 소유 슬롯을 게임 종료 시 반환해야 한다

`RO-02`(1인 1방) + `RO-03`(기존 방 있으면 자동 이동) 때문에 이대로 두면 막힌다.

```
방장이 최종 화면에서 "새 방 만들기" 탭
  → 방금 끝난 방이 아직 살아있음 (RO-14의 30분 소멸 전)
  → RO-03 발동 → 방금 끝난 방의 대기실로 튕김 → 새 방을 못 만듦
```

**해결** — `game:finished` 발행 시점에 방을 `finished`로 바꾸고 **소유 슬롯을 즉시 반환**한다. 방 레코드는 최종 결과를 보는 동안 남아 있다가 RO-14로 소멸한다.

```
수정  RO-13  "방장은 대기실에서 방을 명시적으로 닫을 수 있다"
       → 범위를 넓히고, 게임 종료 시 슬롯 반환이 자동 발동하도록
```

---

## 추후 개발 목록에 추가

- **같은 인원으로 재시작** (구 FN-03) — 방을 유지한 채 포인트만 초기화하고 다시 시작. P2

---

## PRD v1.0에 반영할 변경 요약

### 폐기
```
LK-01 LK-02 LK-05          리액션 규칙 전면 교체 (RX-*로 재정의)
FN-03                      다시하기 → P2
디자인 설렘·당황            표준 FER 라벨에 없음
디자인 CODE 배지            12자 slug로 대체
디자인 건너뛰기·자동 제출     CP-03·RD-06과 충돌
디자인 denied 로스터 항목    PM-07 위반
디자인 방 이름              미채택
Play 대기 화면              RS-01과 충돌
```

### 수정
```
EM-01  감정 7종 라벨 교체
RD-03  기본 30초 → 20초
RD-04  P1 → P0, 15/30/45 → 15/20/30
RO-07  카메라 권한 준비 상태 → 연결 상태
RO-13  대기실 한정 → 게임 종료 시 자동 발동 포함
RS-02  카드 목록 → 캐러셀 + 레일
RS-03  카드 재정렬 → 레일 썸네일 재정렬
RS-07  좋아요 활성화 신호 삭제
RS-08  좋아요 10초 → 감상 시간 인원 비례 + 전원 스킵
SC-06  포인트 = 순위 포인트 + 보너스 → 순위 포인트
FN-02  좋아요 보너스 내역 삭제
5.9절  「좋아요」→「리액션」, LK-* → RX-*
```

### 승격
```
RD-04  P1 → P0   제한시간 방장 선택
LK-08  P1 → P0   "가장 사랑받은 표정" (리액션의 유일한 귀결)
```

### 닫힌 미결
```
13장 1번  혐오·두려움 포함 여부  → 포함 (C-1)
13장 7번  좋아요 배점           → 배점 없음 (C-4)
13장 8번  좋아요 담합           → 점수가 없어 소멸 (C-4)
```

### 열어둔 미결
```
13장 6번   권한 거부자 배제 → 베타에서 이탈률 관찰 (C-7)
13장 9번   먼저 낸 사람의 대기 → 실시간 집계로 채웠으나 관찰 필요 (C-3)
13장 11번  미제출 페널티 강도 → 유지하되 관찰 (C-6)
```

### 신규 이벤트
```
round:skip        C→S   토글
round:skipStatus  S→C   { skipped, total }
```
